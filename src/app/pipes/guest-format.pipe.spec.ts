import { GuestFormatPipe } from './guest-format.pipe';

describe('GuestFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GuestFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
